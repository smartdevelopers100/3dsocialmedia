import React from 'react';
import './Success.css';

function Success({success}) {
    return (
        <div className="success">
            {success}
        </div>
    );
}

export default Success;
